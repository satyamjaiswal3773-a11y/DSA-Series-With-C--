                                       Pattern Problem

Inverted Traingle Pattern :-
-------------------------

1 1 1 1
  2 2 2
    3 3
      4

-----
int n = 4;

int num = 1;
for(int i = 0; i < n; i++){
    // space
    for(int j = 0; j<i; j++){
        cout << " ";
    }
    // num
    for(int j = 0; j<n-i; j++){
        cout << (i+1);
    }
    cout << endl;
}

Pyramid Pattern :-
----------------

    1
  1 2 1
 1 2 3 2 1
1 2 3 4 3 2 1

---
int n = 4;

for(int i = 0; i<n; i++){
    // space
    for(int j = 0; j<n-i-1; j++){
        cout << " ";
    }

    // num 1
    for(int j = 1; j<= i+1; j++){
        cout << j;
    }
    // num 2
    for(int j = i; j>0; j--){
        cout << j;
    }
    cout << endl;
}

Hollow Daimonnd Pattern :-
---------------------

           *                 
        *      *
    *              *
*                      *

    *              *
        *      *
            *
----
For Top--

int n = 4;
for(int i = 0; i<n; i++){
    // space 
    for(int j=0; j<n-i-1; j++){
        cout << " ";
    }
    cout << "*";
    if(i != 0){
        // spaces
        for(int j=0; j<2*i-1; j++){
             cout << " ";
        }
        cout << "*";
    }
    cout << endl;
}

For Bottom :-

for(int i = 0; i<n-1; i++){
    // space
     
    for(int j=0; j<n-i-1; j++){
        cout << " ";
    }
    cout << "*";
    if(i != n-2){
        // space
        for(int j=0; j<2*(n-i)-5; j++){
             cout << " ";
        }
        cout << "*";
    }
    cout << endl;
}


                                                 -: Function :-

1. Find sum of 1 to n numbers ?

#include <iostream>
using namespace std;

int sumN(int n){
    int sum ==0;

    for(int i = 1; i <=n; i++){
        sum += i;
    }

    return sum;
}

int main(){
    cout << sumN(4) <<endl;

    return 0;
}

---------------
2. Calculate N factorial ?

#include <iostream>
using namespace std;

int factorialN(int n){
    int factorial = 1;

    for(int i = 1; i<=n; i++){
        factorial *= i;
    }
    return factorial;
}

int main(){
    cout << factorialN(5) << endl;

    return 0;
}
-------------

3. Calculate sum of digits of a number ?

#include <iostream>
using namespace std;

int sumofDigits(int num){
    int digitSum = 0;

    while(num > 0){
        int lastDigit = num % 10;
        num /= 10;

        digitSum += lastDigit;
    }

    return digitSum;
}

int main(){

    cout << "sum = " << sumofDigits(2356) << endl;
    return 0;
}


4. WAF to check if a number is prime or not ?
5. WAF to print all prime numbers from 1 to N ?
6. WAF to print nth Fibonacci ?
7. Calculate nCr binomial coefficient for n & r ?   [nCr = n! /  r!(n-r)! ]


                                        Binary Number System :-

Decimal to Binary :-                                        
-----------------

#include <iostream>
using namespace std;

int decToBinary(int decNum){
    int ans = 0, power = 1;

    while(decNum > 0){
        int rem = decNum % 2;
        decNum /= 2;

        ans += (rem * power);
        power *= 10;
    }

    return ans;
} 

int main(){
    int decNum = 50;

    cout << decToBinary(decNum) << endl;
    return 0;
}


Binary to Decimal :-
-----------------

#include <iostream>
using namespace std;

int binToDecimal(int binNum){
    int ans = 0, power = 1;

    while(binNum > 0){
        int rem = binNum % 10;
        ans += (rem * power);

        binNum /= 10;
        power *= 2;
    }

    return ans;
} 

int main(){
    int binNum = 110010;

    cout << binToDecimal(binNum) << endl;
    return 0;
}

Two's complement :-
----------------


                                          -:Array Syntax :-
                                    
Find smallest/largest in Array -
-----------

#include <iostream>
using namespace std;

int main(){
    int nums[] = {5,15,22,1,-15,-24};
    int size = 6;

    int smallest = INT_MAX;
    int largest = INT_MIN;

    for(int i = 0; i<size; i++){
        if(nums[i] < smallest){    // to find smallest number
            smallest = nums[i];
        }

        smallest = min(nums[i],smallest);    // to find smallest number
        largest = max(nums[i], largest);   // to find largest number
    }

    cout << "smallest = " << smallest << endl;
    cout << "largest = " << largest << endl;
    return 0;
}

Find Index where smallest and largest value are stored ?
----

Pass by reference :-
-----------------

#include <iostream>
using namespace std;

void changeArray(int arr[], int size){
    cout << "in function\n";
    for(int i=0; i<size; i++){
        arr[i] = 2*arr[i];
    }
}

int main(){
    int arr[] = {1,2,3};

    changeArray(arr,3);

    cout << "in main\n";

    for(int i=0; i<3; i++){
        cout << arr[i] << " ";
    }
    cout << endl;

    return 0;
}

Linear Search :-
--------------

#include <iostream>
using namespace std;

int linearSearch(int arr[],int size,int target){
    for(int i=0; i<size; i++){
        if(arr[i] == target){
            return i;
        }
    }

    return -1;
}

int main(){
    int arr[] = {4,3,7,8,1,2,5};
    int size = 7;
    int target = 8;

    cout << linearSearch(arr,size,target) << endl;

    return 0;
}

Reverse an Array :-  (using to pointer approach)
----------------

#include <iostream>
using namespace std;

void reverseArray(int arr[], int size){
    int start = 0,end= size-1;
    while (start < end)
    {
        swap(arr[start],arr[end]);
        start++;
        end--;
    }
    
}

int main(){
    int arr[]= {2,3,4,5,6,7,8};
    int size = 7;

    reverseArray(arr,size);

    for(int i=0; i<size; i++){
        cout << arr[i] << " ";
    }
    cout << endl;

    return 0;
}

WAF to Calculate sum & product of all numbers in a array ?
WAF to swap the max & min number of an array?
WAF to print all the unique values in an array?
WAF to print intersection of 2 arrays?


                                                    -: Vectors :-

Vector Syntax:-
-----------
           - vector<int>vec;
           - vector<int>vec={1,2,3}
           - vector<int>vec(3,4)

#include <iostream>
#include <vector>       // for using vector be must include this namespace
using namespace std;


int main(){
    vector<char> vec = {'A','B','C','D'};

   for (char val : vec)   // for each loop
   {
        cout << val << endl;
   }
   

    return 0;
}

Vector Functions :-
----------------
                  - size
                  - push_back
                  - pop_back
                  - front
                  - back
                  - at

size :-
----

#include <iostream>
#include <vector>       // for using vector be must include this namespace
using namespace std;


int main(){
    vector<char> vec = {'A','B','C','D'};

    cout << "size = " << vec.size() << endl;

   for (char val : vec)   // for each loop
   {
        cout << val << endl;
   }
   

    return 0;
}

push_back :-
---------

#include <iostream>
#include <vector>       // for using vector be must include this namespace
using namespace std;


int main(){
    vector<int> vec;

    cout << "size = " << vec.size() << endl;
    vec.push_back(25);                           // To add value in last 
    cout << "After = " << vec.size() << endl;

   for (int val : vec)   // for each loop
   {
        cout << val << endl;
   }
   

    return 0;
}

pop_back :-
--------

#include <iostream>
#include <vector>       // for using vector be must include this namespace
using namespace std;


int main(){
    vector<int> vec;

    vec.push_back(25);                           // To add value in last 
    vec.push_back(35);                           
    vec.push_back(45);                           

    cout << "After = " << vec.size() << endl;

    vec.pop_back();           // To remove  value in last 

   for (int val : vec)   // for each loop
   {
        cout << val << endl;
   }
   

    return 0;
}

front  && back  :-
---------------

#include <iostream>
#include <vector>       // for using vector be must include this namespace
using namespace std;


int main(){
    vector<int> vec;

    vec.push_back(25);                           // To add value in last 
    vec.push_back(35);                           
    vec.push_back(45);                           

    cout << "After = " << vec.size() << endl;

    vec.pop_back();           // To remove  value in last 

   cout << vec.front() << endl;   // gives first value

   cout << vec.back() << endl;    // gives last value

    return 0;
}

at :-
---

#include <iostream>
#include <vector>       // for using vector be must include this namespace
using namespace std;


int main(){
    vector<int> vec;

    vec.push_back(25);                           // To add value in last 
    vec.push_back(35);                           
    vec.push_back(45);                           

    cout << "After = " << vec.size() << endl;

    vec.pop_back();           // To remove  value in last 

//    cout << vec.front() << endl;   // gives first value

//    cout << vec.back() << endl;    // gives last value

    cout << vec.at(1) << endl;    // to acces value from index

    return 0;
}

Stactic vs Dynamic Allocation :-
-----------------------------

Single Number ?
-----------
#include <iostream>
#include <vector>       // for using vector be must include this namespace
using namespace std;

int singleNumber(vector<int>& nums){
    int ans = 0;
    for(int val : nums){
        ans ^= val;
    }
    return ans;
}

int main(){
    vector<int> nums = {4, 1, 2, 1, 2};
    cout << singleNumber(nums) << endl;
    return 0;
}

linearSearch code ?
Reverse array code ?      - In function


                        -: Maximum Subarray Sum :-


1. Brute Force Approach :-
                       
#include <iostream>
using namespace std;


int main(){
    int n = 5;
    int arr[5] = {1,2,3,4,5};

    int maxSum = INT32_MIN;

    for (int st = 0; st < n; st++)
    {
        int currSum = 0;
        for (int end = st; end < n; end++)
        {
            currSum += arr[end];
            maxSum = max(currSum, maxSum);
        }
    }

    cout << "max subarray sum = " << maxSum << endl;
    
    return 0;
}

Kadane's Algorithm :- (most Optimised)
-------------------

#include <iostream>
using namespace std;


int main(){
    int n = 7;
    int arr[7] = {3,-4,5,4,-1,7,-8};

    int currSum = 0 , maxSum = INT32_MIN;
    for (int i = 0; i < n; i++)
    {
       currSum += arr[i];
       maxSum = max(currSum, maxSum);

       if (currSum < 0)
       {
            currSum = 0;
       }
       
    }

    
    return 0;
}


